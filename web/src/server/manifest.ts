import manifest from "../../../system/drive-manifest.json";

export const driveManifest = manifest;
export const operationalSheetId = manifest.drive.masterSheet.id;
export const productTruthSheetId = manifest.drive.productTruth.id;
export const productionFolderId = manifest.drive.rootFolder.id;
