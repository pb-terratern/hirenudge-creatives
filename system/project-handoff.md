# HireNudge Creatives project handoff

## Current state

The Week 01 operating system is live in Google Workspace and linked from `drive-manifest.json`.

- The v1 local agent system is implemented at `agents/content-director.md` with specialist contracts under `agents/`, and its canonical controls are in `system/content-gates.json`.
- Local contract tests and the semantic registry validator are implemented. The lean Google tracker migration is complete; the controlled test is not yet complete.
- `HireNudge Content Tracker` is now the operational master. It has four tabs (`LinkedIn`, `Instagram`, `X`, `YouTube`), six columns (`Topic`, `Approach`, `Category`, `Format`, `Status`, `Content Doc`) and no approved topic rows yet.
- The previous `HireNudge Weekly Social Content System` is retained in the same Drive folder as `drive.legacyMasterSheet` for recovery only. Nothing was deleted or migrated from it.

- Week 01 is structure-only. No topic, Concept Key, Hook Key, copy or script has been selected.
- All 22 assets are in `Research backlog`.
- The Week 01 audience is a broad mix of Indian tech and non-tech final-year students, recent graduates and freshers considering global remote and contractor roles.
- The lean launch uses one mandatory anchor each on LinkedIn, Instagram and X. YouTube remains Phase 2 until the post-Day-30 evidence review passes.
- The Sheet is the operational index. The Docs contain research and production detail.
- `HireNudge Product Truth` is live as a separate native Sheet. Its 38 capability rows, 20 claim decisions and 13-source register control product-led wording; it does not replace the content tracker.
- Nudge Studio is recorded feature by feature as `Demonstrated in Development`, based on 20 screenshots of the 7 August 2026 development build. Public release, parsing quality, score validity, job-link fetching and export fidelity remain unverified.
- `Content Registry` is hidden and enforces Concept Key and Hook Key uniqueness.
- The earlier `HIRE NUDGE` project remains a read-only strategy and research reference for this creative workflow.
- `HireNudge Social Media Strategy V2 – August 2026.docx` was evaluated on 11 August 2026 as an advisory input. Its durable product-truth, funnel, CTA and learning-gate controls are incorporated in the native Sheet and linked Docs; the DOCX is not an operational source of truth.

## Next task

Run the controlled test through G3 using the Content Director. The test must use verified product data or another eligible evidence type, present exact channel treatments in chat and stop before Operations creates a test tracker row or Doc.

## Non-negotiables

- Do not invent a job, outcome, recruiter quote, product capability or immigration claim.
- Product modules are optional evidence tools, not compulsory content pillars. Reverify the selected capability and screen before a product-led asset leaves draft.
- For every product-led treatment, check the current Product Truth `Capabilities` row, `Claims & Conflicts` decision, evidence source and verification date before drafting. Preserve the safe wording and limitations.
- Do not write production content before one option is selected.
- Do not move an asset beyond `Options ready` until Concept Key and Hook Key checks pass.
- Keep one Parent Pack ID across same-week channel adaptations.
- Keep published language natural, specific and human-reviewed.

## Verification completed

- `agents/content-director.md`, the five specialist roles, `agents/content-operations-bot.md`, `agents/content-systems-architect.md`, gate guide and handoff contracts exist and pass local contract tests.
- `system/content-gates.json` defines G0–G9, exact outcomes and the only permitted automated transitions; the validator rejects a missing G2 and `Ready` → `Published`.
- `HireNudge Content Tracker` is a native Google Sheet in the project Drive folder. All four tabs retain a native table over `A1:F2`, a frozen header row and a native Status dropdown with `Approved Topic`, `Drafting`, `Review`, `Ready` and `Published`.
- All four live tab reads contain only the six headers; the conversion seed values were cleared. The legacy Sheet remains present and unchanged.
- The controlled test has not yet been completed and no test topic, tracker row or production Doc has been approved by Priyansh.

- Six Sheet tabs exist; five are visible and `Content Registry` is hidden.
- All 22 Asset IDs are unique and linked to the correct production Doc.
- Status dropdowns and frozen header rows are active.
- Same-pack concept adaptations return `OK`.
- A concept reused by another Parent Pack returns `DUPLICATE CONCEPT`.
- Repeated Hook Keys return `DUPLICATE HOOK`.
- Each production Doc links to the master Sheet and research Doc and contains the required six-column timecoded visual table.
- `Channel Plan!A1:F5` records the lean Week 01 treatment and primary learning signal for every channel.
- The Research Doc contains the Strategy V2 operating lens, four complete decision-field sets, a native review-date chip and the international-audience caveat.
- All four production briefs contain the optional product-truth reference, funnel/CTA fields and neutral CTA controls; the YouTube brief additionally contains the Phase 2 gate.
- No Week 01 option was selected, no content was drafted and no asset status was advanced during the Strategy V2 system update.
- The Product Truth Sheet is in the HireNudge Creatives Drive folder with three tabs: `Capabilities`, `Claims & Conflicts` and `Source Register`. Frozen headers, filters, dropdowns, conditional status colours and bounded populated ranges were verified after native conversion.
